{% if fid and fid.trusted_dashboards %}
[federation]
{% for dashboard_url in fid.trusted_dashboards -%}
trusted_dashboard = {{ dashboard_url }}
{% endfor -%}
{% endif %}

{% if fid and fid.openidc -%}
[openid]
remote_id_attribute = HTTP_OIDC_ISS
{% endif -%}
